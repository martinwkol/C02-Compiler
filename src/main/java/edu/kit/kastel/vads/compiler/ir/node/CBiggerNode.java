package edu.kit.kastel.vads.compiler.ir.node;

public final class CBiggerNode extends BinaryOperationNode {
    public CBiggerNode(Block block, Node left, Node right) {
        super(block, left, right);
    }
}

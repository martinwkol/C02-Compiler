package edu.kit.kastel.vads.compiler.ir.node;

public final class CSmallerNode extends BinaryOperationNode {
    public CSmallerNode(Block block, Node left, Node right) {
        super(block, left, right);
    }
}
